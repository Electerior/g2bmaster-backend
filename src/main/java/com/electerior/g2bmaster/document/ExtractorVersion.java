package com.electerior.g2bmaster.document;

/**
 * 첨부 본문을 뽑은 파서의 버전.
 *
 * <p><b>이 문자열이 바뀌면 색인 전체가 재추출 대상이 된다.</b> 그래서 값 자체보다 "언제 올려야
 * 하는가"가 중요하다 — {@code DocumentTextExtractor} 나 그 아래 파서(hwpxlib·hwplib·PDFBox·POI)의
 * <b>출력이 달라질 수 있는</b> 변경을 했으면 올린다. 성능 개선이나 로깅처럼 같은 입력에 같은
 * 출력을 내는 변경은 올리지 않는다.
 *
 * <p>왜 이런 칸이 필요한가. 실측으로 나온 사실이다 — 12일 동안 <b>문서 내용이 바뀐 건 103건 중
 * 0건</b>이었는데, 같은 파일에 추출기만 7/30판 → 8/3판으로 바꾸니 <b>70건 중 38건(54%)</b>의
 * 텍스트가 달라졌다(HWP 중첩표 파싱 수정). 재색인을 부르는 가장 잦은 사유가 원문 변경이 아니라
 * 우리 쪽 파서 변경이라는 뜻이다. 그걸 추적하지 않으면 색인은 서로 다른 파서가 뽑은 텍스트가
 * 뒤섞인 상태로 남고, "왜 이 공고만 표가 안 걸리지" 같은 재현 불가능한 제보로 돌아온다.
 *
 * <p>자동 유도(클래스 해시 등)를 쓰지 않는 이유: 주석 한 줄만 고쳐도 값이 바뀌어 전량 재추출
 * (하루 3,643파일·약 0.9GB)이 걸린다. <b>사람이 의도해서 올리는 편</b>이 안전하다.
 */
public final class ExtractorVersion {

	/**
	 * 올릴 때 날짜와 일련번호를 함께 쓴다. 같은 날 두 번 올릴 수 있어야 한다.
	 *
	 * <p>변경 이력:
	 * <ul>
	 *   <li>{@code 2026-08-11.1} — 최초. hwpxlib 1.0.5 / hwplib 1.1.1 / PDFBox 3.0.3 / POI 5.5.1</li>
	 * </ul>
	 */
	public static final String CURRENT = "2026-08-11.1";

	private ExtractorVersion() {
	}
}
